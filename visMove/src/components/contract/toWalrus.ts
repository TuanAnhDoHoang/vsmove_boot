const AGGREGATOR="https://aggregator.walrus-testnet.walrus.space"
const PUBLISHER="https://publisher.walrus-testnet.walrus.space"

export const publishToWalrus = () => {
    const publishApi = `${PUBLISHER}/v1/blobs -d some string`

}